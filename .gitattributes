# 统一换行符，避免 Windows / Linux 之间来回改整文件
* text=auto eol=lf

# 批处理必须保持 CRLF —— cmd.exe 对 LF 结尾的 .bat 处理不稳
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二进制，别碰
*.png binary
*.apk binary
*.jar binary
*.keystore binary
